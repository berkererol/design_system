import { Meta, Story, Preview, Source } from '@storybook/addon-docs/blocks';
import { DialogStoryExample } from '../components/Dialog/DialogStoryExample';
import { Grid } from '@material-ui/core';

<Meta title="Components/Dialog" component={DialogStoryExample} />

# Dialog

Dialogs should be used whenever an explicit response is needed from the user, and that response is needed before anything else can happen.  
A dialog overlays the screen and is normally used for modal events that require users to take an action before they can proceed.

## Statuses

<Preview>
  <Story name="Statuses">
     <Grid container direction="row" justify="center" spacing={4}>
        <Grid item>
          <DialogStoryExample status="info" />
        </Grid>
        <Grid item>
          <DialogStoryExample status="warning" />
        </Grid>
        <Grid item>
          <DialogStoryExample status="error" />
        </Grid>
        <Grid item>
          <DialogStoryExample status="success" />
        </Grid>
    </Grid>
  </Story>
</Preview>
